services:
  # Web Service
  - type: web
    name: websphere-innovations
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /
